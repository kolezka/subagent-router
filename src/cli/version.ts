// The version `--version` prints and `install` records in the generated plugin manifest. It must
// match package.json; `tests/package.test.ts` fails the build if the two drift.
export const VERSION = '0.1.0';
